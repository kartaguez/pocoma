/**
 * Legacy worker partition contracts. New processing uses {@code PartitionHash} and
 * {@code WorkerSegment}. These types remain until the existing worker loops are replaced.
 */
package com.kartaguez.pocoma.engine.legacy.processing.segmentation;
