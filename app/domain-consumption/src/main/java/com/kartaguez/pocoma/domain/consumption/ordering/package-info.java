/**
 * Explicit and deterministic claim ordering keys for each durable work family.
 *
 * <p>These keys define claim priority, not completion order.</p>
 */
package com.kartaguez.pocoma.domain.consumption.ordering;
