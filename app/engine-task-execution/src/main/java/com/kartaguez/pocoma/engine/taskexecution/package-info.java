/**
 * Functional execution of autonomous pipeline tasks.
 *
 * <p>This package resolves the handler for a task and executes its pipeline-specific use case.
 * Claiming, completion, failure, release, polling, retries, and worker lifecycle belong to
 * technical processing and incoming worker adapters.</p>
 */
package com.kartaguez.pocoma.engine.taskexecution;
